4 layers architecture

UI => components 
   => pages

Hook (for managing state and api layers )
=> hooks
     
State  (data store kaha hoga)
      => auth.context.jsx
      => ai.context.jsx

Api (for communication with backend)
=>services 
       =>auth.api.js